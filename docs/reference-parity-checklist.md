# Reference Parity Checklist — Shubhkamna Events

Tracks parity between the approved reference site (https://dynamo-studio.github.io/Shubhkamna-Events-Demo/) and the rebuilt CMS-driven site. One row per reference section/page. See `docs/reference-site-audit.md` for full seed content, design tokens, and anti-pattern notes behind each row.

| Reference section                                                               | Ticket(s)                    | Status                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Header / nav                                                                    | SHU-006 / SHU-008 / SHU-033  | Done — schema + Header organism, mounted site-wide via `(site)/layout.tsx` (real nav order/CTA from the audit); `/studio` intentionally excluded; SHU-033 added the logo mark, hover/active states, pill CTA button                                                                                                                                                                                                 |
| Footer                                                                          | SHU-007 / SHU-008 / SHU-033  | Done — schema + Footer organism, mounted site-wide via `(site)/layout.tsx` (columns, contact block, social links, live-year copyright); `/studio` intentionally excluded; SHU-033 added the dark-mode background + contact icons matching the reference                                                                                                                                                             |
| Home — Hero                                                                     | SHU-010 / SHU-015            | Done — schema + Hero organism (real headline/subhead/CTAs/background image from the audit, optional video, trust badge sourced from SHU-003), live on `/` via SHU-015's page-builder renderer                                                                                                                                                                                                                       |
| Home — Services/offerings                                                       | SHU-011 / SHU-015            | Done — schema + Services organism (4-category homepage summary, icons/titles/descriptions from the audit, "See All Services" link), live on `/` via SHU-015's page-builder renderer                                                                                                                                                                                                                                 |
| Home — Gallery                                                                  | SHU-012 / SHU-015            | Done — schema + Gallery organism (5 real images from the audit, accessible lightbox with focus trap/Escape/arrow-key nav — fixes the reference's hover-only-caption and no-lightbox gaps, §5 items 5/6), live on `/` via SHU-015's page-builder renderer                                                                                                                                                            |
| Home — Testimonials                                                             | SHU-013 / SHU-015            | Done — schema + Testimonials organism (4 real testimonials from the audit, static grid per the ticket's own note that the reference uses no carousel, initials-avatar fallback, "Read N+ Reviews" link sourced from SHU-003), live on `/` via SHU-015's page-builder renderer                                                                                                                                       |
| Home — Contact/enquiry                                                          | SHU-014 / SHU-015            | Done — schema + ContactSection organism, homepage "banner" variant (heading/intro + phone/WhatsApp CTAs sourced from siteSettings, matching the reference's CTA-teaser), live on `/` via SHU-015's page-builder renderer                                                                                                                                                                                            |
| About page                                                                      | SHU-016                      | Not started                                                                                                                                                                                                                                                                                                                                                                                                         |
| Service/event detail pages                                                      | SHU-017                      | Done — `service` document schema + dynamic `/services/[slug]` route (real content for all 6 services.html categories from the audit: description, features, image, CTA), Service + BreadcrumbList JSON-LD, 404s for unknown slugs; also added a `/services` index (not in SHU-017's original scope, but the header/footer nav's existing "Services" link pointed there and 404'd otherwise)                         |
| Home — About/founder-style section (no dedicated page on reference)             | SHU-016 (gap — see audit §4) | Not started                                                                                                                                                                                                                                                                                                                                                                                                         |
| Home — Trust badges / stats strip (50+ reviews, 5.0★, 24/7, 1000+ events)       | SHU-031                      | Done — `statsSection` schema + `Stats` organism, live on `/` between Hero and Services (there's no About section on this homepage to sit between, per SHU-016's rejection); real 4-item stat strip from the audit, 2-col/4-col responsive grid                                                                                                                                                                      |
| Services page — full 6-service catalog + "Our Process" roadmap                  | SHU-011 / SHU-017            | In progress — SHU-017 shipped the 6-service catalog (`/services` index + per-service detail pages, real content from the audit); "Our Process" roadmap (4-step Consultation → Design → Coordination → Execution) still not started, no ticket currently covers it                                                                                                                                                   |
| Services page — FAQ section                                                     | SHU-027                      | Gap — `faqSection` schema/organism built by SHU-027 is only registered on the Contact page for now (its own technical tasks scope it to "initially SHU-029"); services detail pages ([[SHU-017]]) use fixed-shape `service` documents, not a section-array, so FAQ isn't wired there yet                                                                                                                            |
| Contact page — FAQ section                                                      | SHU-027 / SHU-029            | Done — live on `/contact`, all 6 reconciled questions from the audit rendering correctly                                                                                                                                                                                                                                                                                                                            |
| Contact page — enquiry form (full field set + areas served + registered office) | SHU-014 / SHU-029            | Done — `contactPage` singleton schema (hero, reused `contactSection` "form" variant, reach-out-directly panel, areas served, optional FAQ) + `/contact` route; hero, 8-field form, WhatsApp/Call cards, registered office, and 8-place reconciled areas-served list all live and real; ContactPage/LocalBusiness JSON-LD + breadcrumb                                                                               |
| Portfolio/Gallery page (standalone, 20 images, category tags)                   | SHU-028                      | Done — `galleryPage` singleton schema (reuses SHU-012's `galleryImage` object type, extracted into a shared Sanity type rather than redefined) + `/gallery` route, real 20-image portfolio from the audit, reuses Gallery organism's accessible lightbox — no separate implementation; breadcrumb JSON-LD                                                                                                           |
| Global — floating WhatsApp button + WhatsApp-first contact pattern              | SHU-030                      | Done — floating WhatsApp button (all pages, via SiteShell), Header + Footer WhatsApp links, all built from siteSettings' existing `whatsappNumber` field (no hardcoded numbers); accessible name "Chat on WhatsApp" on every affordance, fixing the reference's icon-only unlabeled button (audit §5 item 2); enquiry form remains the primary channel, WhatsApp stays additive per this ticket's explicit decision |
| Global — Google review rating/count/link (5.0★, 50+ reviews)                    | SHU-003 / SHU-031            | Done — reviewRating/reviewCount/reviewUrl already lived in siteSettings since SHU-003 (consumed by Hero badge, Testimonials link); SHU-031 added the separate, editor-controlled stats strip content (not literally bound to those fields, so the two can be updated independently)                                                                                                                                 |
